const state = {
    book: null
}

const getters = {

}

const actions = {

}

const mutations = {
    SET_BOOK(state,{ book }){
        state.book = book
    }
}

export default {
    state,
    getters,
    actions,
    mutations
}